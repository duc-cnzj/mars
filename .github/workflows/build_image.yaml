name: CI
on: [push]
jobs:
  build-frontend:
    name: build-frontend
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          cache: "yarn"
          cache-dependency-path: frontend/yarn.lock
      - uses: actions/cache@v2
        id: yarn-cache
        with:
          path: |
            frontend/node_modules
          key: ${{ runner.os }}-yarn-${{ hashFiles('frontend/yarn.lock') }}
          restore-keys: |
            ${{ runner.os }}-yarn-
      - uses: borales/actions-yarn@v2.3.0
        with:
          cmd: --cwd frontend install
      - uses: borales/actions-yarn@v2.3.0
        with:
          cmd: --cwd frontend build
      - name: upload build files
        uses: actions/upload-artifact@v2
        with:
          name: frontend_dist_files
          path: |
            frontend/build

  releases:
    needs: [build-frontend]
    name: Release Go Binary
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Download frontend files
        uses: actions/download-artifact@v2
        with:
          name: frontend_dist_files
          path: frontend/build
      - uses: actions/setup-go@v2
        with:
          go-version: '1.17'
      - run: |
          go version
          apt install gcc-8-aarch64-linux-gnu
          make build_linux_amd64
          CC=aarch64-linux-gnu-gcc-8 CGO_LDFLAGS="-static" make build_drawin_arm64
          make build_drawin_amd64
      - name: upload build app
        uses: actions/upload-artifact@v2
        with:
          name: build_app
          path: |
            app-linux-amd64
            app-darwin-amd64
            app-darwin-arm64