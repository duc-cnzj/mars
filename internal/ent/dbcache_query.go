// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/duc-cnzj/mars/v4/internal/ent/dbcache"
	"github.com/duc-cnzj/mars/v4/internal/ent/predicate"
)

// DBCacheQuery is the builder for querying DBCache entities.
type DBCacheQuery struct {
	config
	ctx        *QueryContext
	order      []dbcache.OrderOption
	inters     []Interceptor
	predicates []predicate.DBCache
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DBCacheQuery builder.
func (dcq *DBCacheQuery) Where(ps ...predicate.DBCache) *DBCacheQuery {
	dcq.predicates = append(dcq.predicates, ps...)
	return dcq
}

// Limit the number of records to be returned by this query.
func (dcq *DBCacheQuery) Limit(limit int) *DBCacheQuery {
	dcq.ctx.Limit = &limit
	return dcq
}

// Offset to start from.
func (dcq *DBCacheQuery) Offset(offset int) *DBCacheQuery {
	dcq.ctx.Offset = &offset
	return dcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dcq *DBCacheQuery) Unique(unique bool) *DBCacheQuery {
	dcq.ctx.Unique = &unique
	return dcq
}

// Order specifies how the records should be ordered.
func (dcq *DBCacheQuery) Order(o ...dbcache.OrderOption) *DBCacheQuery {
	dcq.order = append(dcq.order, o...)
	return dcq
}

// First returns the first DBCache entity from the query.
// Returns a *NotFoundError when no DBCache was found.
func (dcq *DBCacheQuery) First(ctx context.Context) (*DBCache, error) {
	nodes, err := dcq.Limit(1).All(setContextOp(ctx, dcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{dbcache.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dcq *DBCacheQuery) FirstX(ctx context.Context) *DBCache {
	node, err := dcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DBCache ID from the query.
// Returns a *NotFoundError when no DBCache ID was found.
func (dcq *DBCacheQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dcq.Limit(1).IDs(setContextOp(ctx, dcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{dbcache.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dcq *DBCacheQuery) FirstIDX(ctx context.Context) int {
	id, err := dcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DBCache entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DBCache entity is found.
// Returns a *NotFoundError when no DBCache entities are found.
func (dcq *DBCacheQuery) Only(ctx context.Context) (*DBCache, error) {
	nodes, err := dcq.Limit(2).All(setContextOp(ctx, dcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{dbcache.Label}
	default:
		return nil, &NotSingularError{dbcache.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dcq *DBCacheQuery) OnlyX(ctx context.Context) *DBCache {
	node, err := dcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DBCache ID in the query.
// Returns a *NotSingularError when more than one DBCache ID is found.
// Returns a *NotFoundError when no entities are found.
func (dcq *DBCacheQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dcq.Limit(2).IDs(setContextOp(ctx, dcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{dbcache.Label}
	default:
		err = &NotSingularError{dbcache.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dcq *DBCacheQuery) OnlyIDX(ctx context.Context) int {
	id, err := dcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DBCaches.
func (dcq *DBCacheQuery) All(ctx context.Context) ([]*DBCache, error) {
	ctx = setContextOp(ctx, dcq.ctx, ent.OpQueryAll)
	if err := dcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DBCache, *DBCacheQuery]()
	return withInterceptors[[]*DBCache](ctx, dcq, qr, dcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dcq *DBCacheQuery) AllX(ctx context.Context) []*DBCache {
	nodes, err := dcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DBCache IDs.
func (dcq *DBCacheQuery) IDs(ctx context.Context) (ids []int, err error) {
	if dcq.ctx.Unique == nil && dcq.path != nil {
		dcq.Unique(true)
	}
	ctx = setContextOp(ctx, dcq.ctx, ent.OpQueryIDs)
	if err = dcq.Select(dbcache.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dcq *DBCacheQuery) IDsX(ctx context.Context) []int {
	ids, err := dcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dcq *DBCacheQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dcq.ctx, ent.OpQueryCount)
	if err := dcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dcq, querierCount[*DBCacheQuery](), dcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dcq *DBCacheQuery) CountX(ctx context.Context) int {
	count, err := dcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dcq *DBCacheQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dcq.ctx, ent.OpQueryExist)
	switch _, err := dcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dcq *DBCacheQuery) ExistX(ctx context.Context) bool {
	exist, err := dcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DBCacheQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dcq *DBCacheQuery) Clone() *DBCacheQuery {
	if dcq == nil {
		return nil
	}
	return &DBCacheQuery{
		config:     dcq.config,
		ctx:        dcq.ctx.Clone(),
		order:      append([]dbcache.OrderOption{}, dcq.order...),
		inters:     append([]Interceptor{}, dcq.inters...),
		predicates: append([]predicate.DBCache{}, dcq.predicates...),
		// clone intermediate query.
		sql:  dcq.sql.Clone(),
		path: dcq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DBCache.Query().
//		GroupBy(dbcache.FieldKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dcq *DBCacheQuery) GroupBy(field string, fields ...string) *DBCacheGroupBy {
	dcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DBCacheGroupBy{build: dcq}
	grbuild.flds = &dcq.ctx.Fields
	grbuild.label = dbcache.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//	}
//
//	client.DBCache.Query().
//		Select(dbcache.FieldKey).
//		Scan(ctx, &v)
func (dcq *DBCacheQuery) Select(fields ...string) *DBCacheSelect {
	dcq.ctx.Fields = append(dcq.ctx.Fields, fields...)
	sbuild := &DBCacheSelect{DBCacheQuery: dcq}
	sbuild.label = dbcache.Label
	sbuild.flds, sbuild.scan = &dcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DBCacheSelect configured with the given aggregations.
func (dcq *DBCacheQuery) Aggregate(fns ...AggregateFunc) *DBCacheSelect {
	return dcq.Select().Aggregate(fns...)
}

func (dcq *DBCacheQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dcq); err != nil {
				return err
			}
		}
	}
	for _, f := range dcq.ctx.Fields {
		if !dbcache.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dcq.path != nil {
		prev, err := dcq.path(ctx)
		if err != nil {
			return err
		}
		dcq.sql = prev
	}
	return nil
}

func (dcq *DBCacheQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DBCache, error) {
	var (
		nodes = []*DBCache{}
		_spec = dcq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DBCache).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DBCache{config: dcq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(dcq.modifiers) > 0 {
		_spec.Modifiers = dcq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (dcq *DBCacheQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dcq.querySpec()
	if len(dcq.modifiers) > 0 {
		_spec.Modifiers = dcq.modifiers
	}
	_spec.Node.Columns = dcq.ctx.Fields
	if len(dcq.ctx.Fields) > 0 {
		_spec.Unique = dcq.ctx.Unique != nil && *dcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dcq.driver, _spec)
}

func (dcq *DBCacheQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(dbcache.Table, dbcache.Columns, sqlgraph.NewFieldSpec(dbcache.FieldID, field.TypeInt))
	_spec.From = dcq.sql
	if unique := dcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dcq.path != nil {
		_spec.Unique = true
	}
	if fields := dcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, dbcache.FieldID)
		for i := range fields {
			if fields[i] != dbcache.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := dcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dcq *DBCacheQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dcq.driver.Dialect())
	t1 := builder.Table(dbcache.Table)
	columns := dcq.ctx.Fields
	if len(columns) == 0 {
		columns = dbcache.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dcq.sql != nil {
		selector = dcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dcq.ctx.Unique != nil && *dcq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range dcq.modifiers {
		m(selector)
	}
	for _, p := range dcq.predicates {
		p(selector)
	}
	for _, p := range dcq.order {
		p(selector)
	}
	if offset := dcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (dcq *DBCacheQuery) ForUpdate(opts ...sql.LockOption) *DBCacheQuery {
	if dcq.driver.Dialect() == dialect.Postgres {
		dcq.Unique(false)
	}
	dcq.modifiers = append(dcq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return dcq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (dcq *DBCacheQuery) ForShare(opts ...sql.LockOption) *DBCacheQuery {
	if dcq.driver.Dialect() == dialect.Postgres {
		dcq.Unique(false)
	}
	dcq.modifiers = append(dcq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return dcq
}

// DBCacheGroupBy is the group-by builder for DBCache entities.
type DBCacheGroupBy struct {
	selector
	build *DBCacheQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dcgb *DBCacheGroupBy) Aggregate(fns ...AggregateFunc) *DBCacheGroupBy {
	dcgb.fns = append(dcgb.fns, fns...)
	return dcgb
}

// Scan applies the selector query and scans the result into the given value.
func (dcgb *DBCacheGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dcgb.build.ctx, ent.OpQueryGroupBy)
	if err := dcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DBCacheQuery, *DBCacheGroupBy](ctx, dcgb.build, dcgb, dcgb.build.inters, v)
}

func (dcgb *DBCacheGroupBy) sqlScan(ctx context.Context, root *DBCacheQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dcgb.fns))
	for _, fn := range dcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dcgb.flds)+len(dcgb.fns))
		for _, f := range *dcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DBCacheSelect is the builder for selecting fields of DBCache entities.
type DBCacheSelect struct {
	*DBCacheQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dcs *DBCacheSelect) Aggregate(fns ...AggregateFunc) *DBCacheSelect {
	dcs.fns = append(dcs.fns, fns...)
	return dcs
}

// Scan applies the selector query and scans the result into the given value.
func (dcs *DBCacheSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dcs.ctx, ent.OpQuerySelect)
	if err := dcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DBCacheQuery, *DBCacheSelect](ctx, dcs.DBCacheQuery, dcs, dcs.inters, v)
}

func (dcs *DBCacheSelect) sqlScan(ctx context.Context, root *DBCacheQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dcs.fns))
	for _, fn := range dcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
