// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/duc-cnzj/mars/v4/internal/ent/cachelock"
	"github.com/duc-cnzj/mars/v4/internal/ent/predicate"
)

// CacheLockQuery is the builder for querying CacheLock entities.
type CacheLockQuery struct {
	config
	ctx        *QueryContext
	order      []cachelock.OrderOption
	inters     []Interceptor
	predicates []predicate.CacheLock
	modifiers  []func(*sql.Selector)
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CacheLockQuery builder.
func (clq *CacheLockQuery) Where(ps ...predicate.CacheLock) *CacheLockQuery {
	clq.predicates = append(clq.predicates, ps...)
	return clq
}

// Limit the number of records to be returned by this query.
func (clq *CacheLockQuery) Limit(limit int) *CacheLockQuery {
	clq.ctx.Limit = &limit
	return clq
}

// Offset to start from.
func (clq *CacheLockQuery) Offset(offset int) *CacheLockQuery {
	clq.ctx.Offset = &offset
	return clq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (clq *CacheLockQuery) Unique(unique bool) *CacheLockQuery {
	clq.ctx.Unique = &unique
	return clq
}

// Order specifies how the records should be ordered.
func (clq *CacheLockQuery) Order(o ...cachelock.OrderOption) *CacheLockQuery {
	clq.order = append(clq.order, o...)
	return clq
}

// First returns the first CacheLock entity from the query.
// Returns a *NotFoundError when no CacheLock was found.
func (clq *CacheLockQuery) First(ctx context.Context) (*CacheLock, error) {
	nodes, err := clq.Limit(1).All(setContextOp(ctx, clq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{cachelock.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (clq *CacheLockQuery) FirstX(ctx context.Context) *CacheLock {
	node, err := clq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CacheLock ID from the query.
// Returns a *NotFoundError when no CacheLock ID was found.
func (clq *CacheLockQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = clq.Limit(1).IDs(setContextOp(ctx, clq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{cachelock.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (clq *CacheLockQuery) FirstIDX(ctx context.Context) int {
	id, err := clq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CacheLock entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CacheLock entity is found.
// Returns a *NotFoundError when no CacheLock entities are found.
func (clq *CacheLockQuery) Only(ctx context.Context) (*CacheLock, error) {
	nodes, err := clq.Limit(2).All(setContextOp(ctx, clq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{cachelock.Label}
	default:
		return nil, &NotSingularError{cachelock.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (clq *CacheLockQuery) OnlyX(ctx context.Context) *CacheLock {
	node, err := clq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CacheLock ID in the query.
// Returns a *NotSingularError when more than one CacheLock ID is found.
// Returns a *NotFoundError when no entities are found.
func (clq *CacheLockQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = clq.Limit(2).IDs(setContextOp(ctx, clq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{cachelock.Label}
	default:
		err = &NotSingularError{cachelock.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (clq *CacheLockQuery) OnlyIDX(ctx context.Context) int {
	id, err := clq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CacheLocks.
func (clq *CacheLockQuery) All(ctx context.Context) ([]*CacheLock, error) {
	ctx = setContextOp(ctx, clq.ctx, ent.OpQueryAll)
	if err := clq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CacheLock, *CacheLockQuery]()
	return withInterceptors[[]*CacheLock](ctx, clq, qr, clq.inters)
}

// AllX is like All, but panics if an error occurs.
func (clq *CacheLockQuery) AllX(ctx context.Context) []*CacheLock {
	nodes, err := clq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CacheLock IDs.
func (clq *CacheLockQuery) IDs(ctx context.Context) (ids []int, err error) {
	if clq.ctx.Unique == nil && clq.path != nil {
		clq.Unique(true)
	}
	ctx = setContextOp(ctx, clq.ctx, ent.OpQueryIDs)
	if err = clq.Select(cachelock.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (clq *CacheLockQuery) IDsX(ctx context.Context) []int {
	ids, err := clq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (clq *CacheLockQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, clq.ctx, ent.OpQueryCount)
	if err := clq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, clq, querierCount[*CacheLockQuery](), clq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (clq *CacheLockQuery) CountX(ctx context.Context) int {
	count, err := clq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (clq *CacheLockQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, clq.ctx, ent.OpQueryExist)
	switch _, err := clq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (clq *CacheLockQuery) ExistX(ctx context.Context) bool {
	exist, err := clq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CacheLockQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (clq *CacheLockQuery) Clone() *CacheLockQuery {
	if clq == nil {
		return nil
	}
	return &CacheLockQuery{
		config:     clq.config,
		ctx:        clq.ctx.Clone(),
		order:      append([]cachelock.OrderOption{}, clq.order...),
		inters:     append([]Interceptor{}, clq.inters...),
		predicates: append([]predicate.CacheLock{}, clq.predicates...),
		// clone intermediate query.
		sql:  clq.sql.Clone(),
		path: clq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CacheLock.Query().
//		GroupBy(cachelock.FieldKey).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (clq *CacheLockQuery) GroupBy(field string, fields ...string) *CacheLockGroupBy {
	clq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CacheLockGroupBy{build: clq}
	grbuild.flds = &clq.ctx.Fields
	grbuild.label = cachelock.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Key string `json:"key,omitempty"`
//	}
//
//	client.CacheLock.Query().
//		Select(cachelock.FieldKey).
//		Scan(ctx, &v)
func (clq *CacheLockQuery) Select(fields ...string) *CacheLockSelect {
	clq.ctx.Fields = append(clq.ctx.Fields, fields...)
	sbuild := &CacheLockSelect{CacheLockQuery: clq}
	sbuild.label = cachelock.Label
	sbuild.flds, sbuild.scan = &clq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CacheLockSelect configured with the given aggregations.
func (clq *CacheLockQuery) Aggregate(fns ...AggregateFunc) *CacheLockSelect {
	return clq.Select().Aggregate(fns...)
}

func (clq *CacheLockQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range clq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, clq); err != nil {
				return err
			}
		}
	}
	for _, f := range clq.ctx.Fields {
		if !cachelock.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if clq.path != nil {
		prev, err := clq.path(ctx)
		if err != nil {
			return err
		}
		clq.sql = prev
	}
	return nil
}

func (clq *CacheLockQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CacheLock, error) {
	var (
		nodes = []*CacheLock{}
		_spec = clq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CacheLock).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CacheLock{config: clq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	if len(clq.modifiers) > 0 {
		_spec.Modifiers = clq.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, clq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (clq *CacheLockQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := clq.querySpec()
	if len(clq.modifiers) > 0 {
		_spec.Modifiers = clq.modifiers
	}
	_spec.Node.Columns = clq.ctx.Fields
	if len(clq.ctx.Fields) > 0 {
		_spec.Unique = clq.ctx.Unique != nil && *clq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, clq.driver, _spec)
}

func (clq *CacheLockQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(cachelock.Table, cachelock.Columns, sqlgraph.NewFieldSpec(cachelock.FieldID, field.TypeInt))
	_spec.From = clq.sql
	if unique := clq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if clq.path != nil {
		_spec.Unique = true
	}
	if fields := clq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, cachelock.FieldID)
		for i := range fields {
			if fields[i] != cachelock.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := clq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := clq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := clq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := clq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (clq *CacheLockQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(clq.driver.Dialect())
	t1 := builder.Table(cachelock.Table)
	columns := clq.ctx.Fields
	if len(columns) == 0 {
		columns = cachelock.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if clq.sql != nil {
		selector = clq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if clq.ctx.Unique != nil && *clq.ctx.Unique {
		selector.Distinct()
	}
	for _, m := range clq.modifiers {
		m(selector)
	}
	for _, p := range clq.predicates {
		p(selector)
	}
	for _, p := range clq.order {
		p(selector)
	}
	if offset := clq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := clq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// ForUpdate locks the selected rows against concurrent updates, and prevent them from being
// updated, deleted or "selected ... for update" by other sessions, until the transaction is
// either committed or rolled-back.
func (clq *CacheLockQuery) ForUpdate(opts ...sql.LockOption) *CacheLockQuery {
	if clq.driver.Dialect() == dialect.Postgres {
		clq.Unique(false)
	}
	clq.modifiers = append(clq.modifiers, func(s *sql.Selector) {
		s.ForUpdate(opts...)
	})
	return clq
}

// ForShare behaves similarly to ForUpdate, except that it acquires a shared mode lock
// on any rows that are read. Other sessions can read the rows, but cannot modify them
// until your transaction commits.
func (clq *CacheLockQuery) ForShare(opts ...sql.LockOption) *CacheLockQuery {
	if clq.driver.Dialect() == dialect.Postgres {
		clq.Unique(false)
	}
	clq.modifiers = append(clq.modifiers, func(s *sql.Selector) {
		s.ForShare(opts...)
	})
	return clq
}

// CacheLockGroupBy is the group-by builder for CacheLock entities.
type CacheLockGroupBy struct {
	selector
	build *CacheLockQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (clgb *CacheLockGroupBy) Aggregate(fns ...AggregateFunc) *CacheLockGroupBy {
	clgb.fns = append(clgb.fns, fns...)
	return clgb
}

// Scan applies the selector query and scans the result into the given value.
func (clgb *CacheLockGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, clgb.build.ctx, ent.OpQueryGroupBy)
	if err := clgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CacheLockQuery, *CacheLockGroupBy](ctx, clgb.build, clgb, clgb.build.inters, v)
}

func (clgb *CacheLockGroupBy) sqlScan(ctx context.Context, root *CacheLockQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(clgb.fns))
	for _, fn := range clgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*clgb.flds)+len(clgb.fns))
		for _, f := range *clgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*clgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := clgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CacheLockSelect is the builder for selecting fields of CacheLock entities.
type CacheLockSelect struct {
	*CacheLockQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (cls *CacheLockSelect) Aggregate(fns ...AggregateFunc) *CacheLockSelect {
	cls.fns = append(cls.fns, fns...)
	return cls
}

// Scan applies the selector query and scans the result into the given value.
func (cls *CacheLockSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, cls.ctx, ent.OpQuerySelect)
	if err := cls.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CacheLockQuery, *CacheLockSelect](ctx, cls.CacheLockQuery, cls, cls.inters, v)
}

func (cls *CacheLockSelect) sqlScan(ctx context.Context, root *CacheLockQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(cls.fns))
	for _, fn := range cls.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*cls.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := cls.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
